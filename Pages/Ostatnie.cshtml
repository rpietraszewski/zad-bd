@page
@model zad3.Pages.OstatnieModel
@{

}
@foreach (var item in Model.People2)
{

}
<table class="table">
    <thead>
        <tr>
            <th>
                    @Html.DisplayNameFor(model => model.People2[0].Id)
            </th>
            <th>
                    @Html.DisplayNameFor(model => model.People2[0].Number)
            </th>
            <th>
                    @Html.DisplayNameFor(model => model.People2[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.People2[0].LastName)
            </th>>
            <th>
                @Html.DisplayNameFor(model => model.People2[0].Now)
            </th>>
            <th>
                Wynik
            </th>>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.People2)
        {
            string parameterValue = item.ToString();
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Number)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Now)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => parameterValue)
                </td>
                <td>
                    <a asp-page="./Usuwanie" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>